import { defineMessages } from 'react-intl'

const PREFIX = 'user.gift_coins'

export default defineMessages({
  csvBatchGift: {
    id: PREFIX + '.csv_batch_gift',
    defaultMessage: 'Csv Batch Gift (Coins)'
  },
  csvBatchGiftLogs: {
    id: PREFIX + '.csv_batch_gift_logs',
    defaultMessage: 'Csv Batch Gift (Coins) Logs'
  }
})
