import React from 'react'
import { storiesOf } from '@storybook/react'
import SideBar from '@src/containers/Layout/components/SideBar'

storiesOf('common components', module).add('Sidebar', () => <SideBar />)
